# TESTING - P0 placeholder

Strategy lives in `Doc/FINRECON_MASTER.md` #20. P0 covers health/build contracts only: Gradle `HealthTest` per service, pytest `test_health.py`, `node --test` for frontend. Phase tests arrive with their phases.
